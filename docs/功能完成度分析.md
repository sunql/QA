# 功能完成度分析

> 对照 `设计01-架构蓝图.md` 与 `设计02-详细设计.md`，盘点当前 QA System 的实现状态。
> 生成日期：2026-08-11 ｜ 更新：2026-08-12（补齐 4 项部分实现功能）

---

## 一、已完整实现 ✅

### 模型网关（Model Router）

| 设计功能 | 实现位置 | 说明 |
|---------|---------|------|
| 模型配置（权重 / 成本阈值 / 启停 / API Key 加密） | `ModelConfigPage.tsx` + `model_config_service.py` | 含 Ollama / OpenAI / Azure / 兼容代理四种 provider |
| 加权随机路由 | `model_router_service.py:124-137` | 权重 0 的模型在存在正权重候选时排除 |
| 成本阈值熔断 | `model_router_service.py:97-102` | 估算 prompt 成本后过滤超阈值候选 |
| 累计成本降级 | `model_router_service.py:66-69` | 会话累计成本超预算时强制最便宜模型 |
| 会话亲和性（前 N 轮固定模型） | `model_router_service.py:72-76` | `affinityTurns` 来自配置 |
| LLM 失败回退 | `selectFallbackModel` | 排除失败模型后选最便宜 |
| 用户手动指定模型（跳过路由） | `ChatRequest.modelId` + `forced` 参数 | 本轮新增，详见 task #78 |

### Token 计量

| 设计功能 | 实现位置 | 说明 |
|---------|---------|------|
| 会话级 token 流水记录 | `token_usage_service.py` | `SessionTokenUsage` 表，含 purpose/model/cost |
| 累计会话成本 | `getSessionCost` / `getSessionTurnCount` | 供路由决策 |
| 按模型汇总 | `summarize` | 用量看板使用 |
| Tokenizer 缓存 | `token_counter/` | tiktoken + heuristic 双策略 |
| 用量看板全局趋势 | `UsagePage.tsx` + `token_usage_service.py` | 4 张汇总卡片 + Token 日趋势折线图 + 模型分布饼图；Phase 3 新增 |

### 本体管理（Harness Ontology）

| 设计功能 | 实现位置 | 说明 |
|---------|---------|------|
| 类 / 属性 / 指标 CRUD | `OntologyPage.tsx` + `ontology_service.py` | REST 形式，非斜杠指令 |
| Neo4j 图存储（真实使用） | `neo4j_client.py` | 每次 CRUD 同步 `MERGE` 节点与边，非桩 |
| 属性外键 `ref_class_id` | `ontology_service.py:182-183` | 创建 `REFERENCES` 边 |
| Milvus 向量存储（真实使用） | `milvus_client.py` | `ontology_embeddings` + `query_embeddings` 双集合 |
| 类继承 `SUBCLASS_OF` 边 | `ontology_service.py` | `_ensureNeo4jClass`/`updateClass` 维护继承边；环检测防死循环 |
| 继承层级 Schema 文本 | `ontology_service.py:buildSchemaText` | 按拓扑序缩进渲染父子类 |
| 前端父类下拉选择器 | `OntologyPage.tsx` | 编辑时排除自身与后代以防成环 |

### NL2SQL + Chart

| 设计功能 | 实现位置 | 说明 |
|---------|---------|------|
| ReAct 两阶段（plan → SQL） | `generateValidatedPlan` + `generateSql` | 结构化 QueryPlan 经 schema 校验后指导生成 SQL |
| SQL 安全校验（禁 DROP/UPDATE） | `business_db_pool.py:56-89` | sqlparse 白名单 + 禁止动词黑名单 |
| 失败重试（带错误反馈） | `generateQueryPlan` / `generateSql` / `generateValidatedPlan` | 各重试 `maxRetries` 次 |
| Schema 自动发现 | `schema_introspection_service.py` | Oracle/PG/MySQL，SHA-256 版本缓存 |
| 多数据源动态切换 | `business_db_pool.py` | 按 datasource_id 缓存 adapter（oracledb / SQLAlchemy） |
| Oracle 版本感知（11g vs 12c+） | `nl2sql_service.resolveDialect` | 11g 用 ROWNUM，12c+ 用 FETCH FIRST |
| 图表渲染 table/bar/pie/line/scatter | `ChartRenderer.tsx` | ECharts (`echarts-for-react`) |
| 数据形态自动推荐图表类型 | `chart_service.recommendChartType` | 1 维+1 指标→饼/柱；时间维→折线；2 维→柱 |
| 用户显式指定图表类型 | `ChatRequest.chartType` + `ChatPanel` Select | 覆盖自动推荐；Phase 2 新增 |
| 意图解析增强（维度/指标/图表类型抽取） | `intent_service.py` | 关键词 + 正则；`IntentResult` 结构化返回 |
| `DEFINE / MAP / METRIC` 领域命令 | `chat_service.py:_handleDomainCommand` | 调用 OntologyService CRUD，零 LLM 消耗 |
| 查询实体抽取 `ExtractedEntities` | `intent_service.py` + `ChatResponse` | 右端匹配 metric 短语；`_DIMENSION_BY_RE` / `_DIMENSION_PER_RE` 抽取 dimension；前端 MessageList 展示 |

### 嵌入 / 向量检索

| 设计功能 | 实现位置 | 说明 |
|---------|---------|------|
| 嵌入生成 | `EmbeddingService.generateEmbedding` | OpenAI text-embedding-3-small（dim=1536） |
| 历史查询向量存储 | `storeQueryEmbedding` | fire-and-forget，失败不阻断主流程 |
| 相似历史查询检索 | `searchSimilarQueries` → `POST /chat/suggest` | 后端已就绪；Phase 4 新增前端展示 UI |
| 相似问法建议 UI | `ChatPanel.tsx` | 300ms 防抖请求相似 chips；点击回填；发送后清空 |

### 前端

| 设计功能 | 实现位置 | 说明 |
|---------|---------|------|
| SSE 流式问答 | `ChatPage.tsx` + `chatStore.ts` | 非流式兜底 |
| 模型选择器（自动路由 + 指定） | `ChatPanel.tsx:78-84` | 本轮新增 |
| 数据源选择器 | `ChatPanel.tsx:30-50` | 自动选默认源 |
| Markdown 渲染 | `MessageItem.tsx` | react-markdown + remark-gfm |
| 查询计划卡片 | `QueryPlanCard.tsx` | 展示 ReAct 计划 |
| SQL 预览 | `SqlPreview.tsx` | 可复制 |
| 主题切换 | `AppLayout.tsx:62-68` | 暗色 / 亮色 |
| 用量看板（会话级 + 全局趋势） | `UsagePage.tsx` | 4 张汇总卡 + 日趋势折线图 + 模型分布饼图；Phase 3 |
| 图表类型选择器 | `ChatPanel.tsx` | 自动/表格/柱状图/饼图/折线图/散点图；Phase 2 |
| 相似历史问法建议 | `ChatPanel.tsx` | 300ms 防抖 chips；点击回填；Phase 4 |
| 斜杠指令 `/define /map /metric` | `intent_service.py` + `chat_service.py:_handleDefineClass` | 零 LLM；Phase 5 |
| 会话亲和性前端展示 | `chat_service.py:_buildAffinityStatus` + `MessageItem.tsx` | 锁定 badge；Phase 7 |
| Monaco Editor SQL 预览 | `SqlPreview.tsx` + `@monaco-editor/react` | 只读 + 复制；Phase 8 |

---

## 二、部分实现（需完善）⚠️

| 功能 | 现状 | 缺口 |
|------|------|------|
| 本体语义检索 | 仅 `searchByEmbedding(vector)`，需调用方先算向量 | 无文本→向量→检索端到端路径（已由本次 Phase 1 补齐，上表已更新） |
| Oracle 版本字段（前端表单） | 后端 `oracle_version` 列已就绪 | 前端 DataSource 表单缺字段选择器（**已由本次 Phase 1 补齐**） |
| 类继承 `parent_class_id` | ✅ 已补齐 | Phase 1 完成 |
| 意图解析 | ✅ 已补齐 | Phase 2 完成 |
| 用量看板全局趋势 | ✅ 已补齐 | Phase 3 完成 |
| 历史相似问题建议 | ✅ 已补齐 | Phase 4 完成 |

---

## 三、未实现 ❌

| 功能                                      | 设计来源                     | 优先级               |
| --------------------------------------- | ------------------------ | ----------------- |
| `/define` `/map` `/metric` 斜杠指令         | 设计01 三、2 + 设计02 二、2      | 中——已有等价 REST CRUD |
| `chartType` 请求参数（用户指定图表）                | 设计02 三、1 Request DTO     | 中                 |
| 会话亲和性前端展示                               | 设计01 三                   | 低——后端已有           |
| 本体版本管理（version / valid_from / valid_to） | 设计02 五、3                 | 低                 |
| Redis 缓存本体                              | 设计02 六                   | 低                 |
| 多租户隔离 tenant_id                         | 设计02 六                   | 低                 |
| 多租户隔离 tenant_id                         | 设计02 六                   | 低                 |

---

## 四、本次补齐项（2026-08-11 ~ 2026-08-12）

| # | 功能 | 关键改动 |
|---|------|---------|
| Phase 1 | 本体类继承 `parent_class_id` 语义化 | Neo4j `SUBCLASS_OF` 边 + 环检测 + 层级缩进 schema text + 前端父类下拉 |
| Phase 2 | 意图解析增强 + DEFINE/MAP/METRIC | `IntentResult` 结构化返回；`_extractMetric` 右端匹配；`_handleDomainCommand` 零 LLM；前端图表类型 Select + `ExtractedEntities` 展示 |
| Phase 3 | 用量看板全局趋势 | `getGlobalSummary`/`getDailyTrends`/`getModelUsage`；`/sessions/usage/global|daily|by-model`；UsagePage 4 张汇总卡 + 折线图 + 饼图 |
| Phase 4 | 历史相似问题建议 UI | `getSuggestions` API；ChatPanel 300ms 防抖相似 chips；点击回填 |
| Phase 5 | 斜杠指令 `/define /map /metric` | `IntentService` 斜杠优先匹配 + `_handleDefineClass`（创建本体类） + 复用 `_handleDefineMetric`/`_handleMapProperty`；3 项意图测试 + 4 项编排测试 + 3 项流式测试 + 1 项 ChatPage 测试；ChatPanel placeholder 提示斜杠示例 |
| Phase 6 | 本体类版本管理 | `OntologyClass` 加 `version`/`valid_from`/`valid_to` + 移除 `class_name` 唯一约束；`updateClass` 创建新版本（INSERT + 旧行 validTo=now）；`listClasses(includeExpired=False)` 默认仅当前版本；新增 `GET /ontology/classes/{name}/versions` 子路由；`deleteClass` 软删除（validTo=now）；Neo4j/Milvus 仍按新 id 覆盖；前端 OntologyPage 列表显示 `v{n}` + 版本切换弹窗 |
| Phase 7 | 会话亲和性前端展示 | `AffinityStatus` schema + `ChatResponse.affinityStatus`；`_buildAffinityStatus` 在 turnCount < N 且锁定同模型时返回 lockedModel + remainingTurns；processMessage + 4 处流式 done 事件携带；前端 MessageItem 显示 `� 锁定 X · 剩 N 轮` Tag；chatStore done 事件回填 |
| Phase 8 | Monaco Editor SQL 预览 | SqlPreview 替换 `<Paragraph code copyable>` 为只读 `@monaco-editor/react`（sql/vs-dark，readOnly+minimap off+fontSize 12+wordWrap on）；右上角自定义复制按钮 `navigator.clipboard.writeText` + 降级 textarea + 失败 toast；setup.ts vi.mock monaco；MessageItem.test.tsx 新增 3 项（渲染/点击复制/失败降级） |
